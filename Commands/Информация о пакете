1) пакет коммандс содержит классы комманд выполняемых в интерактивном режиме
внутри каждого класса комманды создаётся объект класса
CollectionManager
далее создаётся конструктор под именем текущего класса в который
добавляется созданный объект с помощью  this
Затем переопределяется метод execute в котором CM выполняет
метод текущего класса

При этом есть комманды в которых см не нужен например команда exit

В COMMANDS НЕ ПРОПИСЫВАЕТСЯ МЕТОД КОММАНДЫ ТО ЕСТЬ ТО ЧТО КОММАНДА ДЕЛАЕТ
это прописывается в CollectionManager

2) Интерфейс Command содержит метод  boolean execute
3) Абстрактный класс AbstractCommand имплементит интерфейс комманд
